const colors = {
    primaryDark: '#004B94',
    primaryWhite: '#FFFFFF',
}

const fonts = {
    sansSerif: 'sans-serif',
}

export const theme = {
    colors,
    fonts,
}

export type Theme = typeof theme
